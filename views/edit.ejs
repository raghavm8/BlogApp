<!DOCTYPE html>
<html>
    <head>
        <title>BLOGS</title>
        <link rel="stylesheet" type="text/css" href="https://cdnjs.cloudflare.com/ajax/libs/semantic-ui/2.4.1/semantic.min.css">
        <link rel="stylesheet" type="text/css" href="/stylesheets/app.css">
        <style>
        </style>
    </head>
    <body>
        <div class="ui fixed inverted menu">
            <div class="container ui">
                <div class="header item">
                    <i class="code icon" id="raghav"></i>
                    Blog Site
                </div>
                <a href="/" class="item">Home</a>
                <a href="/blogs/new" class="item">New Post</a>
            </div>
        </div>
        <div class="ui main text segment container">    
            <div class="ui huge header">
                <h1 style="text-align: center;"><%= blog.title %></h1>
                <form class="ui form" action="/blogs/<%= blog._id %>?_method=PUT" method="POST">
                    <div class="field">
                        <label>Title</label>
                        <input type="text" value="<%= blog.title %>" name="blog[title]" required>
                    </div>
                    <div class="field">
                        <label>Image</label>
                        <input type="text" value="<%= blog.image %>" name="blog[image]" required>
                    </div>
                    <div class="field">
                        <label>Blog Content</label>
                        <textarea placeholder="enter the content of the blog" name="blog[body]" required><%= blog.body %></textarea>
                    </div>
                    <input class="ui blue big inverted button" type="submit" >
                </form>
            </div>
        </div>
    </body>
</html>